<resources>

    <!-- Application theme. -->
    <style name="my_app_theme" parent="@android:style/Theme.Holo.Light.DarkActionBar">

        <item name="android:actionBarStyle">@style/my_actionbar</item>
    </style>

    <!-- Application theme with no logo -->
    <style name="my_app_theme_no_logo" parent="@android:style/Theme.Holo.Light.DarkActionBar">

        <item name="android:actionBarStyle">@style/my_actionbar_no_logo</item>
    </style>

    <!-- My Action Bar Style -->
    <style name="my_actionbar" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">

        <item name="android:backgroundStacked">@color/color_eap_logo_purple</item>
        <item name="android:background">@color/color_eap_logo_purple</item>
    </style>

    <!-- My Action Bar Style -->
    <style name="my_actionbar_no_logo" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">

        <item name="android:displayOptions">showHome|homeAsUp|showTitle</item>
        <item name="android:backgroundStacked">@color/color_eap_logo_purple</item>
        <item name="android:background">@color/color_eap_logo_purple</item>
        <item name="android:icon">@android:color/transparent</item>
    </style>

    <style name="my_dialog_no_title" parent="@android:style/Theme.Holo.Light.Dialog.NoActionBar"/>

    <!-- Progress Bar Style -->
    <style name="my_progress_dialog" parent="@android:style/Theme.Holo.Light.Dialog.NoActionBar.MinWidth">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTitleStyle">@null</item>
        <item name="android:colorBackground">#ffffff</item>
        <!-- change background color -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:textSize">@dimen/font_size_default</item>
    </style>

</resources>
